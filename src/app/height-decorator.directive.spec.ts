import { HeightDecoratorDirective } from './height-decorator.directive';

describe('HeightDecoratorDirective', () => {
  it('should create an instance', () => {
    const directive = new HeightDecoratorDirective();
    expect(directive).toBeTruthy();
  });
});
